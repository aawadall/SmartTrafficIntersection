{
  "name": "smart-traffic-intersection",
  "version": "0.0.1",
  "description": "Smart Traffic Intersection using Reinforcement learning ML",
  "main": "app.js",
  "scripts": {
    "test": "mocha \"./tests/**/*.spec.js\"",
    "test-with-coverage": "istanbul cover _mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | codacy-coverage && rm -rf ./coverage"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.2.0",
    "codacy-coverage": "^3.4.0",
    "istanbul": "^0.4.5",
    "mocha": "^6.2.0",
    "mocha-lcov-reporter": "^1.3.0"
  }
}
